% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_grouped.R
\name{predict.growth_fit_grouped}
\alias{predict.growth_fit_grouped}
\title{Predict sizes from a growth_fit_grouped object, per group}
\usage{
\method{predict}{growth_fit_grouped}(object, new_t = NULL, group = NULL, n = 200, ...)
}
\arguments{
\item{object}{Object of class \code{"growth_fit_grouped"}.}

\item{new_t}{Vector of ages at which to predict. If \code{NULL}, a
regular grid over the observed range is used.}

\item{group}{Optional character vector restricting the prediction to a
subset of the fitted group levels. By default (\code{NULL}), predicts
for every group.}

\item{n}{Number of grid points if \code{new_t} is \code{NULL}.}

\item{...}{Unused.}
}
\value{
Data frame with columns \code{t}, \code{group}, and \code{y_pred}
  (one row per age x group combination).
}
\description{
Predict sizes from a growth_fit_grouped object, per group
}
\examples{
data(growth_data)
fit <- fit_growth_grouped(growth_data, "age", "size", "group", free = "all")
predict(fit, new_t = c(1, 5, 10))
head(predict(fit, group = "Male"))
}
