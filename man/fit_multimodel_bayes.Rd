% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_bayesian.R
\name{fit_multimodel_bayes}
\alias{fit_multimodel_bayes}
\title{Fit several growth models to the same data by Bayesian inference}
\usage{
fit_multimodel_bayes(
  data,
  age,
  size,
  models = names(GROWTH_MODELS),
  distribution = c("normal", "lognormal"),
  ...
)
}
\arguments{
\item{data}{Data frame with the data.}

\item{age}{Name (character) of the age column in \code{data}.}

\item{size}{Name (character) of the size/weight column in \code{data}.}

\item{models}{Vector of model names to fit (default: all available models).}

\item{distribution}{\code{"normal"} (additive error) or
\code{"lognormal"} (multiplicative error).}

\item{...}{Additional arguments passed to \code{\link{fit_growth_bayes}}.}
}
\value{
Named list of \code{"growth_fit_bayes"} objects (class
  \code{"multimodel_growth_bayes"}).
}
\description{
Bayesian analogue of \code{\link{fit_multimodel}}: fits, in a single
call, a collection of growth models to the same data via
\code{\link{fit_growth_bayes}}. Models that fail (e.g. the sampler
cannot find a finite starting point) are dropped with a warning.
}
\examples{
data(growth_data)
\donttest{
mm_b <- fit_multimodel_bayes(growth_data, "age", "size",
                              models = c("von_bertalanffy", "gompertz"),
                              n_iter = 3000, n_burnin = 800, n_chains = 3, seed = 1)
bayes_table(mm_b)
}
}
