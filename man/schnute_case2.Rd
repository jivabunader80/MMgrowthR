% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_models.R
\name{schnute_case2}
\alias{schnute_case2}
\title{Schnute growth model (case 2: a != 0, b = 0)}
\usage{
schnute_case2(t, a, y1, y2, t1, t2)
}
\arguments{
\item{t}{Vector of ages.}

\item{a}{Parameter related to the growth rate (a != 0).}

\item{y1}{Estimated size at the reference age \code{t1}.}

\item{y2}{Estimated size at the reference age \code{t2}.}

\item{t1}{Smaller (fixed, not estimated) reference age.}

\item{t2}{Larger (fixed, not estimated) reference age.}
}
\value{
Vector of predicted sizes.
}
\description{
Special case of the general Schnute (1981) model when the shape
parameter \code{b} approaches zero (exponential-type growth in size),
keeping the rate parameter \code{a} different from zero. As with the
general case, \code{t1} and \code{t2} are fixed (not estimated)
reference ages.
}
\details{
L(t) = y1 * exp( log(y2/y1) * (1 - exp(-a(t-t1))) / (1 - exp(-a(t2-t1))) )
}
\examples{
schnute_case2(t = 1:10, a = 0.3, y1 = 15, y2 = 80, t1 = 1, t2 = 10)
}
