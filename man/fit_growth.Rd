% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_fitting.R
\name{fit_growth}
\alias{fit_growth}
\title{Fit an age-size growth model}
\usage{
fit_growth(
  data,
  age,
  size,
  model = "von_bertalanffy",
  method = c("ls", "mle"),
  distribution = c("normal", "lognormal"),
  start = NULL,
  control_ls = NULL,
  control_mle = NULL
)
}
\arguments{
\item{data}{Data frame with the data.}

\item{age}{Name (character) of the age column in \code{data}.}

\item{size}{Name (character) of the size/weight column in \code{data}.}

\item{model}{Name of the model to fit (see \code{names(GROWTH_MODELS)}).}

\item{method}{\code{"ls"} (least squares) or \code{"mle"} (maximum likelihood).}

\item{distribution}{For \code{method = "mle"}: \code{"normal"} (additive
error) or \code{"lognormal"} (multiplicative error). Ignored if
\code{method = "ls"}.}

\item{start}{Named list of starting values. If \code{NULL} (default), it
is computed automatically with \code{\link{initial_values}}.}

\item{control_ls}{Control list for \code{minpack.lm::nls.lm.control}.}

\item{control_mle}{Control list for \code{optim} (via \code{bbmle::mle2}).}
}
\value{
An object of class \code{"growth_fit"}.
}
\description{
Fits any of the models in \code{\link{GROWTH_MODELS}} (including
Schnute) to age-size data, either by nonlinear least squares
(\code{method = "ls"}, using \code{minpack.lm::nlsLM}, Levenberg-Marquardt
algorithm) or by maximum likelihood (\code{method = "mle"}, using
\code{bbmle::mle2}), assuming additive normal or multiplicative
lognormal errors.
}
\examples{
data(growth_data)
fit <- fit_growth(growth_data, "age", "size", model = "von_bertalanffy")
summary(fit)

# Maximum likelihood, lognormal errors:
fit_mle <- fit_growth(growth_data, "age", "size", model = "von_bertalanffy",
                       method = "mle", distribution = "lognormal")
fit_mle
}
