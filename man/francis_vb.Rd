% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_models.R
\name{francis_vb}
\alias{francis_vb}
\title{Francis (1988) reparametrisation of von Bertalanffy}
\usage{
francis_vb(t, L1, L2, L3, t1, t2, t3)
}
\arguments{
\item{t}{Vector of ages.}

\item{L1}{Estimated expected size at the reference age \code{t1}.}

\item{L2}{Estimated expected size at the reference age \code{t2}.}

\item{L3}{Estimated expected size at the reference age \code{t3}.}

\item{t1}{Smallest (fixed, not estimated) reference age.}

\item{t2}{Middle (fixed, not estimated) reference age, \code{= (t1+t3)/2}.}

\item{t3}{Largest (fixed, not estimated) reference age.}
}
\value{
Vector of predicted sizes.
}
\description{
Reparametrises the von Bertalanffy model in terms of the expected sizes
\code{L1}, \code{L2}, \code{L3} at three fixed (not estimated) reference
ages \code{t1 < t2 < t3}, with \code{t2 = (t1 + t3) / 2}. Chosen so that
\code{t1}/\code{t3} span the observed age range, this parametrisation is
well known to have much better statistical properties (lower parameter
correlation, more stable convergence) than fitting \code{Linf}/\code{K}/\code{t0}
directly, and to be directly comparable across studies using different
age ranges.
}
\details{
L(t) = L1 + (L3 - L1) * (1 - r^(2*(t - t1)/(t3 - t1))) / (1 - r^2),
with r = (L3 - L2) / (L2 - L1)
}
\examples{
francis_vb(t = 1:10, L1 = 15, L2 = 75, L3 = 84, t1 = 1, t2 = 5.5, t3 = 10)
}
\references{
Francis, R.I.C.C. (1988). Are growth parameters estimated
  from tagging and age-length data comparable? Canadian Journal of
  Fisheries and Aquatic Sciences, 45(6), 936-942.
}
