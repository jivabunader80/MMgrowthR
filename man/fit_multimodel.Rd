% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02_fitting.R
\name{fit_multimodel}
\alias{fit_multimodel}
\title{Fit several growth models to the same data (multi-model)}
\usage{
fit_multimodel(
  data,
  age,
  size,
  models = names(GROWTH_MODELS),
  method = c("ls", "mle"),
  distribution = c("normal", "lognormal"),
  ...
)
}
\arguments{
\item{data}{Data frame with the data.}

\item{age}{Name of the age column.}

\item{size}{Name of the size column.}

\item{models}{Vector of model names to fit (by default, all available
age-size models).}

\item{method}{\code{"ls"} or \code{"mle"}.}

\item{distribution}{\code{"normal"} or \code{"lognormal"} (only if
\code{method = "mle"}).}

\item{...}{Additional arguments passed to \code{\link{fit_growth}}.}
}
\value{
Named list of \code{"growth_fit"} objects (class
  \code{"multimodel_growth"}), in the same order as \code{models}.
}
\description{
Fits, in a single call, a collection of growth models to the same
age-size data. Models that fail to converge are dropped with a
warning, rather than stopping the whole run.
}
\examples{
data(growth_data)
mm <- fit_multimodel(growth_data, "age", "size",
                      models = c("von_bertalanffy", "gompertz", "logistic"))
aic_table(mm)
}
